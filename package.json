{
  "name": "eca-1",
  "version": "0.4.0",
  "private": true,
  "description": "Universal governed robotics intelligence platform with portable software-first deployment across heterogeneous physical embodiments.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/**/*.test.ts",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
